__pycache__/
.coverage
.claude/worktrees/

# EJ module lives in 9va4admin/atm-ej-platform — not this repo
modules/ej/
apps/ej_ingestion/
node_modules/
dist/
*.local

# Pilot secrets — generated by scripts/dev-init.py, never commit
scripts/.env.dev
scripts/.env.ps1
secrets/
infra/astra.env
dist/

# ML model weights (large binaries — distributed via MLflow, not git)
apps/sig_detector/weights/
apps/*/weights/

# Test digest reports (generated artifacts — not source)
test-reports/

# Debug and scratch outputs (generated during development)
debug_*.jpg
debug_*.png
_test_*.png
_test_*.jpg

# Log files
*.log

# Temp test-build helpers (content already merged into proper test files)
tests/**/partial_image_tests_append.py

# Edge scanner agent — compiled Windows binaries (distributed via release, not git)
edge/cts-scanner-agent/*.exe
# Canon SDK sample cheques captured during local dev/testing — not committed
edge/cts-scanner-agent/scannned-chqs/
